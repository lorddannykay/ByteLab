/**
 * TypeScript type definitions for course template generation
 * Re-exports types from the main course types to ensure compatibility
 */

export type {
  CourseData,
  CourseConfig,
  CourseStage,
  StageContent,
  ContentSection,
  ImageMetadata,
  InteractiveElement,
  QuizQuestion,
  SideCardContent,
} from '@/types/course';
